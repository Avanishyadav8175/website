// components
import Input from "@/lib/Forms/Input/Input";

// types
import { type BlogArticleDocument } from "@/common/types/documentation/blog/blogArticle";

export default function TableFormFields({
  initialDocument
}: {
  initialDocument?: BlogArticleDocument;
}) {
  return <></>;
}
